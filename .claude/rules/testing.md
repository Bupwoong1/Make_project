# Testing Rules (Auto-loaded every session)

- Never mark a test as passing without evidence (actual output or validation result).
- Flask API testing: use curl or Python requests, verify JSON response structure.
- Blueprint validation: check JSON syntax, module connections, variable references.
- HTML templates: verify well-formed HTML, check template variables, test responsiveness.
- Reference: `docs/3-reference/how-to-test-make-project.md`
